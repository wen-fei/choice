<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.choice.dao.CommonDao">
    <delete id="deleteOne">
      DELETE FROM #{tableName}
      where #{rowName} = #{rowId}
    </delete>


</mapper>